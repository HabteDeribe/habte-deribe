package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Elegant deep forest teal and mint emerald colors for H-Ethio-land style
val Emerald80 = Color(0xFFE6F3F2) // Light cyan/teal badge background (0xFFE6F3F2)
val Emerald40 = Color(0xFF0C625F) // Main brand deep teal color from the screenshots

// Bright mint teal accents
val TealAccent = Color(0xFF14B8A6)
val TealSoft = Color(0xFFF0FDF4)

// Gold / Amber (Property, Wealth, Sales, Action)
val Gold80 = Color(0xFFFEF3C7)
val Gold40 = Color(0xFFD97706)

// Primary Light Slate and pure white palette
val CharcoalDark = Color(0xFF0E1D20) // Deep dark text charcoal/teal
val CharcoalLight = Color(0xFFFFFFFF) // Pure white card surfaces
val CharcoalSurface = Color(0xFFF8FAFC) // Very light slate-gray background
val LightSlate = Color(0xFFF1F5F9) // Mid light slate gray
val CardSlate = Color(0xFFE2E8F0) // Subtle outline border slate

// Accent / Violation Warning Red
val WarningRed = Color(0xFFEF4444)
val WarningRedSoft = Color(0xFFFEE2E2)


